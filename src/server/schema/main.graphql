type Query {
    teams: [Team]
    messages: [Message]
}

type Team {
    rank: Int
    name: String
    score: Int
    lastSubmission: String
}

type Message {
    posted: String
    content: String
}